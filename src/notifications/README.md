# Notification Platform
Centralized notification system consuming EventBus events.
Priority Queue → Channel Router → Toast / In-App / (Future: Email, SMS, Push).
Never create toasts directly inside pages or components.
